'use strict';

/**
 * Copyright (c) Xinhuanet Inc. All rights reserved.
 *
 * @License: MIT
 * @Author: SuperWoods
 * @Email:  st_sister@iCloud.com
 * @Date:   2016-08-09-06:45:42
 *
 * @(demo)Last modified by:   SuperWoods
 * @(demo)Last modified time: 2016-08-18-08:28:38
 */

$(function () {
    // 开发模块
    var $tempShow = $('#tempShow');
    // 临时DOM
    var $tempDOM = $('#tempDOM');

    // 隐藏临时DOM和开发模块
    // $tempShow.show();
    // $tempDOM.show();

    // 克隆input 到 tempDOM
    var cloneDOM = function cloneDOM($tempDOM, DOM) {
        $tempDOM.html(DOM);
    };

    // attr
    var attr = '';
    var $attr = $('#attr');
    var $attrText = $attr.find('.attr');
    var $nameAttrArray = $attr.find('[name="attr"]');

    // 是否折行 （‘\n’）
    var $format = $('#format');
    var $formatInput = $format.find('input');
    var wrap = '';
    $formatInput.eq(0).on('click', function (e) {
        wrap = '\n';
    });
    $formatInput.eq(1).on('click', function (e) {
        wrap = '';
    });

    // 判断是否有 class
    var classHandler = function classHandler($tag) {
        var className = $tag.attr('class');
        return className && className.length > 0 ? ' class="' + className + '"' : '';
    };

    // 主处理器 killHandler
    var killHandler = function killHandler($tempDOM) {
        var $output = $('#output');
        var nodeid = $('#nodeid').val();
        var repeat = $('#repeat').val();
        var dayspan = repeat < 200 ? 0 : repeat;

        // cms 规则
        var cms = {
            begin: '<!--webbot bot="AdvTitleList" nodeid="' + (nodeid === '' ? '888888' : nodeid) + '" type="0" spanmode="0" dayspan="' + dayspan + '" attr="' + attr + '" comstring="',
            lt: '&lt;',
            gt: '&gt;',
            enpquot: '#enpquot#',
            Picture: {
                needcode: ['<img data-src="<Picture needcode=0>PictureUrlPh</Picture>">', '<img data-original="<Picture needcode=0>PictureUrlPh</Picture>">',
                // '<img data-src="<Picture needcode=0>PictureUrlPh</Picture>" width="' + imgWidth + '" height="' + imgHeight + '" alt="<Title length="0">TitlePh</Title>">',
                '<Picture needcode=1 width=100 height=100>PictureUrlPh</Picture>']
            },
            Title: '<Title length="0">TitlePh</Title>',
            ArticleUrlPh: 'ArticleUrlPh',
            Abstract: '<Abstract>AbstractPh</Abstract>',
            Subtitle: '<Subtitle>SubtitlePh</Subtitle>',
            end: '" TAG="BODY" PREVIEW="[高级标题列表]" artattr="0" isshowcode="0" titlekeyword="" keyword="" tagstring="00" starttime="" endtime="" id="" startspan --><!--webbot bot="AdvTitleList" endspan i-checksum="0" -->'
        };

        // 预处理 tempDOM
        // console.log('orgHtml0:', $tempDOM.html());
        var $a = $tempDOM.find('a');
        var $abs = $tempDOM.find('.abs');
        var $subTitle = $tempDOM.find('.subTitle');
        var $ul = $tempDOM.find('ul');

        var $img = $tempDOM.find('img');
        var imgWidth = $img.attr('width');
        var imgHeight = $img.attr('height');

        // 处理 a 内部内容
        // let aHandler = () => {
        $a.each(function (i, e) {
            var $this = $(e);
            console.log('$a0:', $this.find('img').size() === 0);
            console.log('$a1:', $this.text().indexOf('详细') !== 0);
            if ($this.find('img').size() === 0 && $this.text().indexOf('详细') !== 0) {
                $this.text(cms.Title);
            }
            // else {
            //     let html = $.trim($(e).html());
            //     console.log(html);
            //     $this.html(html);
            // }
        });

        // 处理 $abs
        $abs.each(function (i, e) {
            var $this = $(e);
            // console.log('$abs0:', $this.find('a').size() > 0);
            if ($this.find('a').size() > 0) {
                var aText = $this.find('a').text();
                console.log('$abs1:', aText);
                // $this.text(cms.Abstract + 'URL_BEGIN<a href="' + cms.ArticleUrlPh + '">URL_END' + aText + '</a>');
                $this.text(cms.Abstract + 'URL_BEGIN<a href="' + cms.ArticleUrlPh + '">URL_END' + aText + '</a>');
            } else {
                $this.text(cms.Abstract);
            }
        });

        // 处理 $subTitle
        $subTitle.each(function (i, e) {
            var $this = $(e);
            if ($this.find('a').size() > 0) {
                $this.text('URL_BEGIN<a href="' + cms.ArticleUrlPh + '">URL_END' + cms.Subtitle + '</a>');
            } else {
                $this.text(cms.Subtitle);
            }
        });

        // 处理 a href自身
        $a.attr('href', cms.ArticleUrlPh).before('URL_BEGIN').prepend('URL_END');

        // 处理 img
        $img.each(function (i, e) {
            var $this = $(e);
            $this.replaceWith(function () {
                var temp = cms.Picture.needcode[2];
                if ($this.attr('data-src')) {
                    temp = cms.Picture.needcode[0];
                } else if ($this.attr('data-original')) {
                    temp = cms.Picture.needcode[1];
                }
                return temp;
            });
        });

        // 获取预处理过的 tempDOM
        var orgHtml = $tempDOM.html();

        // 开发模块：展示处理后的DOM
        var showDOM = function showDOM(DOM) {
            $tempShow.text(DOM);
        };
        showDOM(orgHtml);

        // 处理 li DOM
        var $li = $ul.find('li');
        var size = $li.size();
        console.log(size);

        if ($ul && size > 1) {
            // 处理 ul 的 class
            var ulClass = classHandler($ul);
            console.log(ulClass);

            // 处理 li 的 class
            var liClass = classHandler($li);
            console.log(liClass);

            // 转换 orgHtml
            orgHtml = '<ul' + ulClass + '>\n                    <Repeat Begin=0 End=' + size + '>\n                        <Article>\n                            <li' + liClass + '>' + $li.eq(0).html() + '</li>\n                        </Article>\n                    </Repeat>\n                </ul>';
        } else {
            // 添加 Article
            orgHtml = '<Article>' + orgHtml + '</Article>';
            // 添加 Repeat
            if (repeat !== '' && repeat > 0) {
                orgHtml = '<Repeat Begin=0 End=' + repeat + '>' + orgHtml + '</Repeat>';
            }
        }

        // 格式化 orgHtml
        orgHtml = $.HTMLFormat(orgHtml, {
            indent_size: wrap === '\n' ? 4 : 0,
            wrap: wrap === '\n' ? true : false,
            indent_character: wrap === '\n' ? ' ' : ''
        });
        console.log('HTMLFormat orgHtml: ', orgHtml);
        // console.log('orgHtml1:', orgHtml);

        // 使用正则转码
        orgHtml = orgHtml.replace(/picture/g, 'Picture').replace(/URL_BEGIN/g, '<Url>').replace(/URL_END/g, '</Url>').replace(/</g, cms.lt).replace(/>/g, cms.gt).replace(/"/g, cms.enpquot);

        console.log('newHtml:', orgHtml);

        // 清空 outputTemp
        var outputTemp = '';

        // 重新给 outputTemp 赋值
        outputTemp = cms.begin + wrap + orgHtml + wrap + cms.end;

        console.log(outputTemp);

        // 输出结果（必须是字符串）
        $output.text(outputTemp);
    };

    // 初始化激活 默认属性
    $nameAttrArray.eq(0)[0].checked = true;

    // 属性处理器
    var attrHandler = function attrHandler(e) {
        $nameAttrArray.eq(0)[0].checked = false;

        var $this = $(e.currentTarget);
        var v = '+' + $this.val();
        console.log(v);

        console.log($this[0].checked && v !== '+');

        if ($this[0].checked && v !== '+') {
            $this.attr('data-attr', v);
        } else {
            $this.attr('data-attr', '');
        }
        //
        // if (attr !== '' || v !== '+') {
        //     $attrText.text(attr);
        // } else {
        //     $attrText.text('默认');
        // }
    };

    // 当属性改变时
    $nameAttrArray.on('change', function (e) {
        var $this = $(e.currentTarget);
        if ($this.val() !== '') {
            attrHandler(e);
        } else {
            // 点击默认列表关闭其它属性
            $nameAttrArray.attr('data-attr', '').each(function (i, e) {
                var $this = $(e);
                if (i === 0) {
                    $this[0].checked = true;
                } else {
                    $this[0].checked = false;
                }
            });
        }
    });

    // 提交
    var $submit = $('#submit');
    $submit.on('click', function () {

        var $input = $('#input');
        var inputVal = $input.val();

        // 克隆 input 内容转为 killHandler 可以处理的 DOM结构
        cloneDOM($tempDOM, $.trim(inputVal));

        // 收集输出 attr
        attr = '';
        $('#attr').find('input').each(function (i, e) {
            var v = $(e).attr('data-attr');
            console.log(v);
            if (v) {
                attr += v;
            }
        });
        console.log('attr:', attr);

        // 转译 tempDOM 并输出
        killHandler($tempDOM);
    });

    // footer添加版本信息，读取 package.json 中 version对象
    $.when($.ajax('package.json')).then(function (data) {
        var v = data.version;
        $('title').append('_v' + v);
        $('footer').append(' _v ' + v);
    });
});

// var people = ['Wayou', 'John', 'Sherlock'];
// //sayHello函数本来接收三个单独的参数人妖，人二和人三
// function sayHello(people1, people2, people3) {
//     `${people1}`
//     console.log(`Hello ${people1},${people2},${people3}`);
// }
// //但是我们将一个数组以拓展参数的形式传递，它能很好地映射到每个单独的参数
// sayHello(...people); //输出：Hello Wayou,John,Sherlock
// `${people}`
